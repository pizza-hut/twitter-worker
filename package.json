{
  "name": "tweet-worker",
  "version": "0.0.1",
  "description": "tweet worker that always listen to tweets",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "fs": "0.0.1-security",
    "https": "^1.0.0",
    "node-cron": "^2.0.3"
  }
  "engines": {
    "node": "10.x"
  }
}
